# MNIST Neural Network

## Set up

```sh
git clone https://github.com/reizt/mnist-discriminator
cd mnist-discriminator
pip install pipenv
pipenv --python 3.11
pipenv install
pipenv run download
```

## Train and Test

```sh
pipenv run test
```
